EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Rooted NestJS backend (POS integration). Leave blank to hide POS features.
# Use a LAN IP / tunnel (not localhost) so a device/simulator can reach it.
EXPO_PUBLIC_API_URL=http://192.168.1.10:4000

# Hosted HTTPS bridge for email links (required for reliable reset/confirm on device).
# Upload docs/supabase/auth-redirect.html to Supabase Storage bucket "auth", then set:
EXPO_PUBLIC_AUTH_REDIRECT_URL=https://your-project-ref.supabase.co/storage/v1/object/public/auth/auth-redirect.html

# Public web app URL (for privacy/terms links in mobile auth screens).
# EXPO_PUBLIC_WEB_URL=https://yourdomain.com
# Or set explicit legal URLs:
# EXPO_PUBLIC_PRIVACY_URL=https://yourdomain.com/privacy
# EXPO_PUBLIC_TERMS_URL=https://yourdomain.com/terms
# EXPO_PUBLIC_SUPPORT_URL=mailto:support@yourdomain.com
