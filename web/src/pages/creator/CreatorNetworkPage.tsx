import { VendorNetworkPage } from '@/pages/vendor/VendorNetworkPage';

/** Creator-shell route for V2V network (stays on `/creator/network`). */
export function CreatorNetworkPage() {
  return <VendorNetworkPage />;
}
