import { VendorInboxPage } from '@/pages/vendor/VendorInboxPage';

/** Creator-shell route for inbox (stays on `/creator/inbox`). */
export function CreatorInboxPage() {
  return <VendorInboxPage />;
}
