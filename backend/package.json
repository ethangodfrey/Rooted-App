{
  "name": "@vendorly/backend",
  "version": "0.1.0",
  "description": "Vendorly backend service (POS integrations, analytics ingestion).",
  "private": true,
  "scripts": {
    "prebuild": "prisma generate",
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main.js",
    "railway:build": "npm run build",
    "railway:start": "npm run start:prod",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "schema:sync": "prisma generate",
    "audit:phase42": "node scripts/audit-phase42-runbook.cjs",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "test": "jest",
    "test:e2e:platform": "jest --config ./test/jest-e2e.json --runInBand test/platform.e2e-spec.ts",
    "markets:links": "ts-node scripts/run-market-links.ts",
    "markets:dedupe": "ts-node scripts/run-dedupe-markets.ts",
    "markets:enrich": "ts-node scripts/run-markets-enrich.ts",
    "markets:fix-times": "ts-node scripts/run-markets-fix-times.ts",
    "markets:schedule:ai": "ts-node scripts/run-schedule-ai-enrich.ts",
    "markets:classify": "ts-node scripts/run-market-classify.ts",
    "markets:usda:fix-schedules": "ts-node scripts/run-usda-schedule-fix.ts",
    "markets:images": "ts-node scripts/run-markets-images.ts",
    "markets:images:google": "ts-node scripts/run-markets-images-google.ts",
    "markets:images:purge": "ts-node scripts/run-markets-images-purge.ts",
    "admin:agent": "ts-node scripts/run-admin-agent.ts",
    "admin:posts": "ts-node scripts/run-admin-post-agent.ts",
    "pos:inventory-worker": "ts-node scripts/run-pos-inventory-worker.ts",
    "seed:stress-transactions": "ts-node scripts/seed-stress-transactions.ts",
    "seed:local-network": "npm run db:seed:network --prefix ..",
    "db:seed:network": "npm run db:seed:network --prefix .."
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.19.2",
    "@nestjs/bullmq": "^11.0.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@nestjs/schedule": "^6.0.0",
    "@prisma/client": "^6.2.0",
    "@supabase/supabase-js": "^2.110.2",
    "@vendorly/env-config": "file:../packages/env-config",
    "axios": "^1.7.9",
    "bullmq": "^5.34.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "geo-tz": "^8.1.4",
    "ioredis": "^5.11.1",
    "jose": "^6.2.3",
    "luxon": "^3.6.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@types/express": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/luxon": "^3.6.2",
    "@types/node": "^22.10.0",
    "jest": "^30.4.2",
    "prisma": "^6.2.0",
    "ts-jest": "^29.4.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
